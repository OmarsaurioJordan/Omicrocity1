//instancia cacino que llama
with(ciudadano){
    if(estado == "libre" and irandom(2) == 0){
        if(point_distance(x, y, argument0.x, argument0.y) < vision){
            s_actividades(id, "baila");
        }
    }
}
